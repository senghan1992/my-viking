# Design — "지식의 지도" (Cartographer's Study)

<!-- impeccable:design-schema 1 -->

## World

지식 도서관을 **지도 제작실(cartographer's study)**로 읽는다. 서가는 지도, 프로젝트는 지역,
지식 권은 지도 위의 표식이다. 브랜드(myviking = 항해자)와 지도·항해의 결합은 제품 은유를
강화하고, "고급스럽고 심플하며 직관적인" 요구를 다음 문법으로 응답한다:

- **운모지 바탕 + 경위선 난(欄)** — 세계의 재료 질감 (장식용 UI 그리드가 아님, DESIGN 근거 보유)
- **먹색 잉크** — 프라이머리 버튼·브랜드·본문 (테라코타·시그널 레드 사용 안 함)
- **놋쇠 컴퍼스 브라스** — 유일한 신호색. 브랜드 마크·포커스·언더라인·판모서리 눈금
- **판(plate) 문법** — 헤어라인 테두리, 인증·모달·증서에는 대각 눈금(브라스 1.5px)
- **EB Garamond 600 (자체 호스팅 woff2)** — 라틴만: 워드마크·색인 숫자·통계 숫자·단계 번호
- **Pretendard Variable (자체 호스팅 woff2, 100–900)** — 한국어·라틴 UI 공용 산스. OS 에 상관없이 같은 글자 사이·같은 웨이트 단계
- **한국어 표제는 반드시 산스 700 + 타이트한 자간** — 세리프는 라틴·숫자 전용 (한글 세리프 폴백 금지: 화면이 '어중간한 시스템 세리프'로 흐려짐)
- **단일 SVG 아이콘 시스템** — 24×24, 1.5 스트로크, 라운드 캡 (이모지 사용 금지)
- **지도 표식** — ■ 확립 · ● 검증 전 · ◆ 검증 필요 (상태는 색+형태 이중 코딩)

## Tokens

| Token | 값 | 용도 |
|---|---|---|
| `--paper` | `#f1ede2` | 바탕 |
| `--surface` | `#fbf8f0` | 판/카드 |
| `--ink` | `#221f18` | 본문·프라이머리 버튼 |
| `--muted` | `#6f6752` | 보조 텍스트 (종이 위 4.8:1 이상) |
| `--line` / `--line-2` | `#dcd3bf` / `#c9bea4` | 헤어라인 |
| `--brass` / `--brass-ink` | `#8a6b2f` / `#6c5224` | 신호색·링크 |
| `--green` `--amber` `--red` (bg 포함) | 먹물 계열 상태색 | 확립/검증 전/검증 필요 |

대비: 핵심 16쌍 전부 WCAG AA(≥4.5) 기계 검증 완료. 선택·캐럿·스크롤바·포커스 링·tabular-nums
까지 재료에서 파생. 대형 텍스트·테이블 숫자·시계열은 `tabular-nums`.

## Type

- UI: **Pretendard Variable 셀프호스팅** (한 파일 100–900, ~2MB woff2) — 시스템 폴백은 -apple-system · Apple SD Gothic Neo · Noto Sans KR · Malgun Gothic
- 한국어 표제: 산스 **700**, 자간 −0.02em, `text-wrap: balance` — 세리프/명조 혼용 금지 (한글은 시스템 세리프로 떨어져 어중간해짐)
- 디스플레이: **EB Garamond 600** (라틴만, 25KB woff2 자체 호스팅) — 워드마크·통계 숫자·책 인덱스·단계 번호
- 모노: 시스템 모노 — 키·코드·slug·판독 데이터
- 본문: 15px/1.65, −0.005em; 계층 = h1 26/700 · h2 16/700 · h3 14/650
- 표제 균형: `text-wrap: balance`, 표제 간격 = 위 넉넉·아래 조금

## Components

- **판(card)**: surface + 헤어라인 + `--shadow-sm`(offset+blur). 인증·모달·키 증서만 대각 눈금 판
- **버튼**: ink 프라이머리 / 헤어라인 세컨더리 / ghost / danger(레드 틴트). 아이콘+텍스트 인라인
- **칩**: 상태 틴트 바탕 + 어두운 먹물 텍스트, 아이콘 포함
- **탭(이용자/모델·카테고리)**: 헤어라인 밑줄 탭, 활성 = 브라스 밑줄
- **서가 카드**: 왼쪽 5px 먹색 등뼈(호버 시 브라스) + 브라스 1px 테두리, `No.N` 세리프 색인
- **통계**: 한 판 안에서 헤어라인으로 나뉜 4칸 원장(ledger) — 대형 세리프 tabular 숫자
- **지식 카드**: 1px 왼쪽 여백 + 상태 표식(■●◆), 헤어라인, 밑줄 텍스트 액션
- **코드/명령 판**: 먹색 판 + 브라스 상단 규칙 (지도 범례식)
- **모달**: 판 문법, 내부 720px, rise 애니메이션

## Motion

하나의 저자의 순간: 판 등장 `rise`(0.4s, cubic-bezier e-아웃, opacity+translate 결합).
상호작용 문법: 카드 호버 리프트(3px)·탭/링크/버튼 전환은 0.15s 등급. 토스트는 슬라이드 인.
정크 없음, 스크롤 트리거 없음.

## Signature Interactions

- 서가 카드 호버: 먹색 등뼈 → 브라스로 교체 (지도 표식이 밝아지는 징후)
- 상태 표식: 색+형태 이중 코딩 (색각 약자 포함)
- 판모서리 눈금: 지도 판 문법의 재귀 신호 (인증·모달·증서)

## Raising (challenger donations)

- **JACKFIELD RAISE**: 상태는 색 단독이 아니라 선/형태 문법으로 구분 (■●◆ 표식, 밑줄 탭)
- **GAUGE RAISE**: 숫자는 탭ular 세리프로 "값 자체가 무게로 읽히도록" (추세 화살표는 데이터가
  없어 추가하지 않음 — 허위 데이터 금지)
- **LEXICON RAISE**: 컨트롤 전부 한 "인쇄 표시" 문법 — 밑줄 텍스트 버튼·헤어라인 칩·탭
- **DECK RAISE**: 포커스는 `:focus-visible` 브라스 링 + 오프셋으로 물리적 위치 대응

## Verification

- 테스트 33개 통과, WCAG 대비 16쌍 기계 검증, detect.mjs 경고 0 (제외: grid advisory —
  지도지 재료로 의도된 소수 표면 문법, DESIGN 참조)
- 첫 배포 화면: 경위선 종이 위 중앙 판 — 컴퍼스 마크, EB Garamond 워드마크, 폼, 먹색 CTA

## Anti-decisions (명시적으로 거부한 것)

- 크림+세리프+테라코타 "북월드 클리셰" (상태색은 먹물 계열, CTA는 먹색)
- 네온/다크 대시보드 — 사용 환경(주간 사무/연구실)이 명암을 결정
- 이모지 아이콘 — 전부 SVG 드로잉으로 대체
- 카드 4px 컬러 좌측 보더 — 지도 표식 + 칩으로 대체
- 키커/아이브로 라벨 — 표제가 스스로 말한다